apps:
  my-app-web:
    run: "/bin/sh -c 'exit 3'"

build:
  method: docker
  dockerfile: docker/Dockerfile
  context: .
  env:
    raw:
      TESTING_ENV: working

release:
  run: "echo hello stefan"
  config:
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
